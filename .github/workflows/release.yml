name: release-please

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  release_please:
    name: release
    uses: cloudnationhq/terraform-azure-workflows/.github/workflows/release.yml@main
