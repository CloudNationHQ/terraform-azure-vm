name: "linting"

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize

jobs:
  checks:
    uses: cloudnationhq/terraform-azure-workflows/.github/workflows/linting.yml@main
